# Workspace Instructions

- Keep the landing page simple, responsive, and easy to deploy.
- Prefer clear sections, strong hierarchy, and lightweight React components.
- Avoid introducing extra dependencies unless they are required.
- Keep changes focused on the current mockup and do not reintroduce the default Vite demo.